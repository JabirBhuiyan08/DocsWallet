import { app } from "../firebase/firebase.config";
import { GoogleAuthProvider } from "firebase/auth";


//to do Make the backend server and the database 
// Todo use axios to send the data to the server

